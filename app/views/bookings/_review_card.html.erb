<div class="container">
  <div class="row justify-content-center py-2">
    <div class="col-md-12">
      <%= simple_form_for [ printer, review ] do |f| %>
        <%= f.input :text, label: false %>
        <%= f.button :submit, "Add Review", class: "btn-flat" %>
      <% end %>
    </div>
  </div>
</div>